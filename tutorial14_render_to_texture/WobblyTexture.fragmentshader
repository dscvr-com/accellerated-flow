#version 330 core

in vec2 UV;

out vec3 color;

uniform sampler2D src;

void main(){
	vec4 cur = texture(src, UV);
    int mul = 1;
	color = vec3(abs(cur.x) * mul, abs(cur.y) * mul, abs(cur.z) * mul);
}